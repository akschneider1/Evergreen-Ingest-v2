model_id: gpt-4o-mini
extraction_passes: 3
max_workers: 4
max_output_tokens: 8192  # Increase if seeing truncated JSON errors
max_char_buffer: 6000    # Smaller chunks = less output per call = fewer truncations
domains:
  - tax
  - benefits
output_dir: output
sources_dir: sources
upload_max_bytes: 5242880  # 5 MB
